%h1 New <%= singular_name %>
= render :partial => 'form', :locals => {:<%= singular_name %> => @<%= singular_name %>, :submit => 'submit'}
= link_to 'Back', <%= path_for(table_name, singular_name, "index") %> 
