%h1 Editing <%= singular_name %>

= render :partial => 'form', :locals => {:<%= singular_name %> => @<%= singular_name %>, :submit => 'update'}

= link_to 'Show', <%= path_for(table_name, singular_name, "show") %>
= link_to 'Back', <%= path_for(table_name, singular_name, "index") %>

